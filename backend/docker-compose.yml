services:
  ledger:
    build:
      context: ..
      dockerfile: backend/Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - ledger-data:/data
    restart: unless-stopped

volumes:
  ledger-data:
